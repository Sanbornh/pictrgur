<%= form_for @picture do |f| %>
	<%= f.label "title" %>
	<%= f.text_field "title" %><br>

	<%= f.label "artist" %>
	<%= f.text_field "artist" %><br>

	<%= f.label :name, "URL" %>
	<%= f.text_field "url" %><br>

	<%= f.submit "SUBMIT" %>
<% end %>
